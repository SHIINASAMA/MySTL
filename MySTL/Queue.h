#pragma once
#include "LinkedList.h"

namespace mystl
{
	template<typename type>
	class Queue : LinkedList<type>
	{
	public:
		//将一个元素放入队列末尾
		void put(type data)
		{
		}

		//返回队列第一个元素
		void peek(int index)
		{
		}
	};
}
